# Features

User-facing behavior is organized by feature. Menu browsing, item details, search, and cart code should live here rather than inside route files. Route files compose features but do not own business rules.
