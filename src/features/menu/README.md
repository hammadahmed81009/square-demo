# Menu feature

This boundary will own location selection, category browsing, search, item cards, item details, and presentation of availability state. It may consume public DTOs but must not import `src/server`.
