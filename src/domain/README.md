# Domain

Pure, framework-independent menu, money, availability, inventory, modifier, and cart rules live here. Domain modules accept explicit inputs such as clocks and timezones and must not read environment variables or browser globals.
